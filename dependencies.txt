# Requires Python 3.10, 3.11, or 3.12 (not 3.13)
transformers>=5.1.0
torch
pinecone==6.0.0
python-dotenv==1.0.0
# sentence-transformers removed; use transformers for embedding models
# torch is required for most models; install manually if needed for GPU/CPU compatibility
